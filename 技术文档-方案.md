# 技术文档（技术方案）

> 项目名：AI 自学阅读器（AI 自学阅读器 + AI 助教）
> 版本：v0.6　更新日期：2026-08-23　状态：已与 PRD v0.3 对齐；UI 定稿 V2 温暖纸感（§1.2），新增侧栏树/拖拽/页面尺寸自适应与模型/API 设置（§1.4、§5.8）
> 前置：先读《需求文档-PRD.md》。本文档覆盖 P0/P1，P2 仅预留接口。

---

## 1. 技术选型

| 层 | 选型 | 备选 | 理由 |
|----|------|------|------|
| 前端框架 | React 19 + Vite + TypeScript | Vue3 | shadcn 生态、AI chat 参考、assistant-ui 组件全在 React；AI HTML 原型迁移损耗最小 |
| UI 组件 | Tailwind v4 + shadcn/ui | Naive UI | 2025-26 审美标杆；组件=源码可全量自定义；CSS 变量深色模式 |
| PDF 渲染 | react-pdf（pdf.js 封装，MIT） | react-pdf-viewer | 堆叠 Page 实现连续滚动；onRenderSuccess 按页回调是勾选页交互地基；**每页按上传时存库的实际宽高比渲染**（PPT 16:9 与 A4 竖版混排无违和） |
| 流式对话 | Vercel AI SDK useChat | 自写 SSE 解析 | 打字机/中断/多轮开箱即用；OpenAI-compatible provider |
| PPT/Word 转换 | LibreOffice headless 转 PDF | Office 自动化 | 一条转换链覆盖两种格式；1 slide = 1 page，页码定位天然成立 |
| 逐页文本抽取 | PyMuPDF (fitz) | pdfplumber | 按页抽文本、与页码天然对齐、速度快 |
| 语言检测 | 词频比例阈值（自写 20 行） | langdetect | 只需区分中文/英文页，不值得引库 |
| 后端 | FastAPI (Python) | | 异步流式响应简单，和 AI SDK 同生态 |
| 推理模型 | OpenAI-compatible Chat API（用户自配） | | 讲解、解题、纠错对话全走这里；用户填写自己的 Base URL / API Key / 模型名 |
| 识图模型 | OpenAI-compatible 视觉模型（示例：vision-model） | 示例：backup-vision-model | 只用于空文本页读图兜底，成本可控 |
| 生图模型 | OpenAI-compatible 生图模型（示例：image-model / banana 等） | 示例：backup-image-model | 画图题 P2 才启用；接入走 providers.py 同一套接口 |
| 数据存储 | SQLite + 本地文件 | | 科目/课件/对话的关系存储，零运维 |

### 1.2 UI 视觉规范（2026-08-17 定稿：V2 温暖纸感）

视觉基准：V2 温暖纸感（米白纸纹 + 衬线标题 + 暖棕强调）。实现时色板/字体以 CSS 变量集中管理（Tailwind v4 `@theme`），值如下：

| Token | 值 | 用途 |
|---|---|---|
| `--bg` | `#f6efe2` | 主画布 · 暖米白 |
| `--sidebar` | `#efe6d1` | 侧栏 · 略深旧纸 |
| `--canvas` | `#eae0c9` | 阅读区 · 桌面纸垫 |
| `--paper` | `#fbf7ec` | 页面纸/卡片 · 象牙白 |
| `--paper-2` | `#f4ecda` | 内嵌卡片/公式底 |
| `--ink / --ink-2 / --ink-3` | `#3c3326 / #6d6150 / #9a8c74` | 主/次/弱化文字（暖深棕，非纯黑） |
| `--border / --border-hi` | `rgba(94,72,45,.14) / .24` | 细边框 · 淡墨线 |
| `--accent` 系 | `#a4683a`（hover `#b57a46`、press `#864f26`、tint 10%/20%） | 暖棕强调：勾选态/按钮/链接 |
| 功能色 | 绿 `#5d7d52` / 红 `#b04a38` / 金 `#c9a24b` | 完成/错误/标记 |
| 圆角 | `--radius 8px` / `--radius-lg 12px` | 统一圆角两级 |
| 阴影 | 暖棕基调双层（见原型 `--shadow-1/2`） | 克制，忌冷灰阴影 |
| 字体 | 标题衬线 `Noto Serif SC`；正文 `Noto Sans SC`；公式 `Source Serif 4` | 三套字体栈见原型 `:root` |
| 纸感 | SVG feTurbulence 细噪点层（`--grain`，alpha 0.05） | body 与页面卡片各挂一层 |
| 布局 | 三栏 `252px / 1fr / 372px`，8px 网格间距节奏 | 可微调，比例保持 |

约束：勾选态 = 暖棕描边 + 10% tint 蒙层 + 左上角勾角标记（原型已定义，迁移时保持）。深色模式暂不做（护眼暖色已达标），接口用 CSS 变量预留。

### 1.3 模型矩阵（多模型路由）

本项目依赖**三类模型能力**，接入层按「能力角色」设计，不把任何模型名写死在业务代码里：

| 能力角色 | 用途 | 主选 | 备选 | 触发时机 |
|----------|------|------|------|----------|
| `chat` | 讲解/解题/纠错对话 | 示例：chat-model | 示例：backup-chat-model | 所有 /chat 请求 |
| `vision` | 空文本页读图（公式/图表/扫描件） | 示例：vision-model（api.example.com/v1） | 示例：backup-vision-model | 上传时检测到空文本页，或用户手动触发重读 |
| `image_gen` | 画图题出图 | 示例：image-model / banana（api.example.com/v1） | 示例：backup-image-model | P2：用户问画图类问题 |

> **P2.1 补充**：函数图 = matplotlib 精确绘制（程序化采样，零 token、秒出、直线严格过原点），波形图/真值表/逻辑门/电路图 = matplotlib + schemdraw 精确绘制；仅几何/立体/实物示意走用户配置的生图模型（原 `[DRAW:]` 路径）。chat 输出类型化标记 `[PLOT: {"kind": "function"|"waveform"|"boolean"|"circuit"|"geometry"|"other", "instruction": "..."}]`，按 kind 分派渲染引擎；绘制链路任何失败自动降级到用户配置的生图模型并在卡片标注「降级生成」。

设计约定：
- 三类角色在页面中独立配置（key/base_url/model 三元组），保存到本地 SQLite；换模型只改页面配置
- `chat` / `vision` / `image_gen` 均通过页面配置接入；是否免费、限额和计费方式由用户选择的 provider 决定
- 部分 provider 或网关可能要求额外请求头，`providers.py` 的 provider 配置结构支持 `extra_headers` 字段
- 路由逻辑集中在 `providers.py`，业务代码只调 `get_provider("chat")` 这类接口
- **需开工时确认**：所选 provider 的稳定性与限流策略；读图失败时可配置备用视觉模型

### 1.4 模型/API 设置模块（P2，高级设置）

目标：以「后端可管理、前端可设置、按能力路由」的模型配置中心作为唯一运行时配置源。普通用户不用修改 `.env`；高级用户可以自己接入 OpenAI-compatible 中转站、官方兼容 API 或本地 OpenAI-compatible 服务。

设计边界：
- 不做模型市场、不自动拉取所有模型列表、不做计费统计、不做团队共享 Key
- P2 只做本地单用户配置，重点是可配置、可测试、可降级、安全不泄露
- 前端入口弱化：侧栏底部/右上角「设置 → 模型与 API」，避免破坏主学习动线

配置结构：
```text
Provider
- id / name / type(openai_compatible | dashscope | zhipu | custom)
- base_url
- api_key_encrypted 或 api_key_ref
- auth_type(bearer | api_key_header | custom_headers)
- extra_headers_json
- enabled / note / created_at / updated_at

ModelConfig
- id / provider_id
- model_name
- capability(chat | vision | image_gen)
- enabled
- priority
- temperature / max_tokens 等可选参数
- note / created_at / updated_at
```

运行时策略：
- `chat`、`vision`、`image_gen` 三类能力分别读取启用模型，按 `priority` 从小到大排序
- 默认调用第一优先级（`priority ASC, id ASC`）；当前版本请求失败直接返回错误，不自动尝试下一模型
- `/api/chat` 可选接收 `model_config_id`，用于当前对话临时指定一条 `chat` 配置；后端按配置 ID 精确构造 Provider/client，并校验配置存在、capability 为 chat、模型启用、Provider 启用。任何校验失败都明确拒绝且不回退默认模型
- AI 对话栏从 `GET /api/model-configs?capability=chat` 与 Provider 列表构建真实选项，只展示模型与 Provider 均启用的配置；默认选择与后端相同的 `priority ASC, id ASC` 第一项，设置弹窗关闭或窗口重新聚焦时刷新
- 业务层只调用 `get_client("chat")` / `get_client("vision")` / `get_client("image_gen")`，不感知具体 Provider
- DB/UI 是唯一运行时配置源：某能力没有启用模型时抛出可操作的配置错误，明确引导「设置 → 模型与 API」，绝不回退 `.env`
- 兼容升级只做一次迁移：启动时仅当 `model_providers` 与 `model_configs` 均为空，读取旧 `.env` 的 `CHAT_*` / `VISION_*` / `IMAGE_GEN_*` 三元组及有效 `*_EXTRA_HEADERS`，事务写入 Provider/Model 后记录持久迁移标志
- 迁移幂等且保护用户配置：重复启动不重复导入；只要已有任意页面配置就跳过迁移，不覆盖、不重排；`.env` 保留为回滚材料，但后续调用不再读取

安全策略：
- 后端接口永不返回完整 API Key，只返回 `masked_key`
- `extra_headers` 中名称包含 Authorization/API-Key/Token/Secret/Password/Credential 的值同样掩码；编辑时空值或原样掩码保留旧 secret
- 前端输入 Key 后只能新增/覆盖，不能查看明文
- 导出配置默认移除密钥字段
- 保密审计要求：`.env`、本地数据库、密钥缓存不进 Git

## 2. 系统架构

```
┌───────────────────────────────────────────────┐
│ 浏览器（前端 SPA）                              │
│  ├─ 科目/文件夹/课件树（拖拽排序、跨科目移动、新建文件夹）│
│  ├─ 文档预览区（react-pdf 连续滚动，按页实际宽高比渲染，│
│  │             点击=勾选页，PPT/A4 混排自适应）         │
│  └─ AI 对话区（SSE 流式；页码可点击回看历史对话）│
└──────────────┬────────────────────────────────┘
               │ HTTP / SSE
┌──────────────▼────────────────────────────────┐
│ 后端 FastAPI                                   │
│  ├─ /subjects      科目 CRUD                   │
│  ├─ /upload        接文件 → 转 PDF → 逐页抽文本 │
│  │                 → 逐页语言检测 → 入库        │
│  ├─ /chat          按 pages[] 取页面文本拼 prompt│
│  │                 → 调模型 → SSE 流式返回      │
│  ├─ /conversations 对话保存 / 按课件页码回查    │
│  └─ /docs/{id}.pdf 静态提供 PDF                │
└───────┬──────────────┬────────────────────────┘
        │              │
   SQLite + 文件    providers.py 模型路由
   (data/)          ├─ chat:      chat-model / backup-chat-model（示例）
                    ├─ vision:    vision-model / backup-vision-model（示例）
                    └─ image_gen: image-model / banana（示例）
```

关键决策记录：
- **页面文本在上传时预抽取**，不在提问时重复解析：100 页课件勾任意页都是查库拼接，响应快
- **语言在上传时逐页检测存库**：提问时按勾选页的语言标记决定是否注入双语指令，不靠模型临场判断
- **PPT 转 PDF 后页码与幻灯片一一对应**，「点击页码回看对话」的定位才成立
- **多模型按能力路由而非单模型包打**：讲解要强推理、空文本页要识图、画图题要生图，三者最佳模型不同且价格差异大；按角色路由让每类调用走性价比最优的模型，且任何一路挂了不影响其他路
- **前端定 React 弃 Vue**：vue-pdf-embed 单人维护且 pdf.js 版本长期滞后（本项目核心交互依赖按页回调，不敢押注）；React 线 react-pdf 活跃且 AI 聊天参考（lobe-chat / Vercel ai-chatbot / assistant-ui）全部现成
- **连续滚动用堆叠 Page 自实现**：滚动容器堆叠 N 个 Page + IntersectionObserver 报当前页（约 20 行）+ 每页 div onClick 勾选；100 页以上加懒挂载。不引入大而全的 viewer 框架

## 3. 核心数据流

```
科目：新建科目 → POST /subjects → subjects 表

上传：选文件 → POST /upload（带 subject_id）
    → originals/ 存原文件
    → LibreOffice 转 PDF 存 pdfs/（PDF 直接跳过）
    → PyMuPDF 逐页抽文本 → 逐页语言检测 → document_pages 表
    → 空文本页：截该页为 PNG → vision 模型读图 → 回填文本（source='vision'）
      （M1 先只标记 empty_pages 返回前端，读图链路 M2 接入）
    → 返回 {doc_id, page_count}

提问：点击预览页（可多选，前端高亮勾选态）
    → POST /chat {doc_id, pages[], question, history, model_config_id?}
    → 后端查 document_pages 取文本 → 按语言标记拼双语指令
    → 系统 prompt + 页面文本 + 历史 + 问题 → 流式调模型 → SSE

存档：对话结束 → 用户点"保存本次对话"
    → POST /conversations {doc_id, pages[], messages}
    → conversations 表

回看：复习时点击课件某页码
    → GET /conversations?doc_id=&page= → 展示关联对话
```

## 4. 数据模型（SQLite）

```sql
subjects(id, name, sort_order, created_at)

folders(id, subject_id, parent_id NULL, name, sort_order, created_at)
-- 科目内文件夹，可嵌套（限制 ≤2 层，后端校验）；parent_id=NULL 即科目根级

documents(id, subject_id, folder_id NULL, filename, orig_path, pdf_path,
          page_count, sort_order, created_at)
-- folder_id NULL = 直接挂在科目下；移动课件 = 改 subject_id/folder_id，不动 doc_id

document_pages(doc_id, page_no, text, lang, source, width, height)
-- lang: 'zh' | 'en'，上传时检测写入
-- source: 'text'（原生文本层）| 'vision'（读图回填）
-- width/height: 每页实际尺寸（pt），上传时 PyMuPDF 读取；前端按 width/height 比例渲染页面卡
-- 联合主键 (doc_id, page_no)

conversations(id, doc_id, pages,   -- pages 存 JSON 数组如 [12,13]
              messages,            -- JSON：完整对话记录
              created_at)
-- 回看时按 pages 包含某页查询（SQLite json_each）
-- 对话只关联 doc_id：课件移动科目/文件夹后历史对话不丢

model_providers(id, name, type, base_url, api_key_ciphertext,
                auth_type, extra_headers_json, enabled,
                note, created_at, updated_at)
-- API Key 不向前端回显；P2 本地版可先用系统 keyring/本地加密文件，简化版至少保证接口不返回明文

model_configs(id, provider_id, model_name, capability,
              enabled, priority, params_json, note,
              created_at, updated_at)
-- capability: 'chat' | 'vision' | 'image_gen'
-- priority: 数值越小优先级越高；同能力下至少一个 enabled 模型才算配置完整
```

## 5. 接口设计

### POST /api/subjects
- 入参：`{ "name": "高等数学" }`
- 出参：`{ "subject_id": "s01" }`

### GET /api/subjects
- 出参：`[{ "id": "s01", "name": "高等数学", "doc_count": 3 }]`（按 sort_order 排序）

### POST /api/folders
- 入参：`{ "subject_id": "s01", "parent_id": null, "name": "第一章" }`
- 出参：`{ "folder_id": "f01" }`；嵌套超过 2 层返回 400

### GET /api/tree?subject_id=
- 出参：科目下的文件夹树 + 课件列表（含 folder_id、sort_order），侧栏整棵树一次拉全

### PATCH /api/subjects/{id}
- 入参：`{ "sort_order": 2 }` 或 `{ "name": "…" }`（拖拽排序落库）

### PATCH /api/documents/{id}
- 入参（三选一或组合）：`{ "folder_id": "f01" }`（拖入文件夹）、`{ "subject_id": "s02" }`（跨科目移动）、`{ "sort_order": 3 }`；跨科目移动时后端清空 folder_id 并校验归属一致性

### DELETE /api/folders/{id}
- 约定：只删空文件夹；非空返回 409 + 子项数量，前端提示「先移走内容」

### DELETE /api/subjects/{id}
- 约定：只删空科目（无课件无文件夹）；非空返回 409 + 统计，前端给「移动内容后删除」引导。不做级联删除，防误删整科课件与对话

### POST /api/upload
- 入参：multipart，`subject_id` + `文件`（pdf/pptx/docx，≤100MB）+ 可选 `folder_id`（上传到指定文件夹，不传=科目根级）
- 出参：`{ "doc_id": "a3f2", "page_count": 42, "en_pages": 40, "vision_pages": [7, 8] }`（en_pages = 英文页数；vision_pages = 读图回填的页，前端可标注"AI 识别"）
- 错误：415 格式不支持；500 转换失败（返回原因）；部分页无文本层时附 `empty_pages: [7,8]` 警告

### POST /api/chat
- 入参：
```json
{
  "doc_id": "a3f2",
  "pages": [12, 13],
  "question": "这两页讲的换元法，第二步为什么这么换？",
  "model_config_id": 12,
  "history": [
    {"role": "user", "content": "……"},
    {"role": "assistant", "content": "……"}
  ]
}
```
- 出参：SSE 流，事件：`delta`（正文片段）/ `done`（含 usage）/ `error`
- 约定：`history` 由前端维护，截断最近 N=10 轮防超限；页面文本由后端注入，**前端不传页面内容**
- `model_config_id` 可选：不传时按 `priority ASC, id ASC` 选择默认启用 chat 模型；传入时按该 ID 精确调用。配置不存在、非 chat、模型禁用或 Provider 禁用时返回明确 4xx 错误，不自动回退默认模型；该字段只影响 chat 推理

### POST /api/conversations
- 入参：`{ "doc_id", "pages", "messages" }`
- 出参：`{ "conv_id": "c77" }`

### GET /api/conversations?doc_id=&page=
- 出参：该课件与指定页关联的对话列表（按时间倒序）

### GET /api/docs/{doc_id}
- 出参：application/pdf 文件流

### GET /api/model-providers
- 出参：Provider 列表，包含 `masked_key`，不包含完整 `api_key`

### POST /api/model-providers
- 入参：`{ "name", "type", "base_url", "api_key", "auth_type", "extra_headers", "enabled", "note" }`
- 出参：`{ "provider_id": "mp01" }`

### PATCH /api/model-providers/{id}
- 入参：任意可编辑字段；`api_key` 为空/缺省时不覆盖旧 Key
- 出参：更新后的 Provider（Key 仍为掩码）

### DELETE /api/model-providers/{id}
- 约定：若已有模型配置引用该 Provider，返回 409；前端提示先删除/迁移模型

### GET /api/model-configs?capability=
- 出参：模型配置列表，可按 `chat` / `vision` / `image_gen` 过滤

### POST /api/model-configs
- 入参：`{ "provider_id", "model_name", "capability", "enabled", "priority", "params", "note" }`
- 出参：`{ "model_config_id": "mc01" }`

### PATCH /api/model-configs/{id}
- 入参：模型名、用途、启用状态、优先级、参数等任意组合

### DELETE /api/model-configs/{id}
- 删除指定模型配置；若删除后某能力没有任何启用模型，前端给出警告

### POST /api/model-configs/test
- 入参：
```json
{
  "capability": "chat",
  "provider_id": "mp01",
  "model_name": "deepseek-chat",
  "sample": "optional test prompt or image"
}
```
- 出参：`{ "ok": true, "latency_ms": 842, "message": "连接成功", "sample_output": "..." }`
- 错误：返回 `{ "ok": false, "error_type": "auth_error|model_not_found|timeout|network|bad_response", "message": "..." }`

## 6. 提示词设计（与 PRD 4.4 对齐）

系统提示词 v1（结构，开工后迭代措辞）：

```
你是面向大学生自学课件的助教，学生没有老师指导。

【输入】系统会注入学生勾选页码的课件内容（含页码标注）。
只依据勾选页内容+自身知识回答；引用课件原文标注页码（如「见 P12」），
不臆造课件中没有的内容。

【流程】
1. 判断问题类型：学知识点 / 解题 / 讨论纠错，选择对应结构
2. 涉及基础知识点时，先以「前置回顾」小节回顾前置知识，再讲新知识
3. 解题型：复述题目 → 思路分析（引用用到的知识点）→ 分步骤推导
   （每步编号+依据）→ 结论；多解法全展示并比较；有公式化套路则
   总结成模板并说明如何识别这类题

【语言】若注入的课件页标记为英文：关键知识点中英对照，标准解答
至少含全英文版本，解题思路至少含全中文版本。中文页正常用中文。

【纠错】学生质疑时先自查推理与识别，错了明确承认并给修正版，
不确定就说明哪部分不确定，禁止嘴硬敷衍。

【关联】结合会话历史，当前问题与之前相似/相反时主动指出并讲区分。

【纪律】步骤编号；公式用 LaTeX；不灌水；答案与讲解分层，
方便复习时快速定位。

【公式协议】行内公式使用 `$...$`；块级公式使用独占行的 `$$...$$`，
且公式块前后留空行。禁止使用 `\(...\)` / `\[...\]`，禁止把公式放进
反引号或代码围栏；输出前检查定界符与花括号闭合，并只用 KaTeX 支持的标准命令。
```

- 后端拼装顺序：系统提示词 + 双语指令（按勾选页语言，任一英文页即触发）+ 页面文本 + history + question
- 前端公式链路：`normalizeMathMarkdown` 仅在代码区域外把 `\(...\)` / `\[...\]`
  归一化为 `$...$` / 独占行 `$$...$$`，为块级公式补空行，并在 SSE 流式期间暂时隐藏
  尾部未闭合定界符；inline code、fenced code、转义美元与货币文本保持原样。
- KaTeX 使用非抛错模式；不支持的命令保留可读源码并以弱化文本色展示，不阻断整条回答。
- `<开工后在此记录实测调优：不同模型对指令的服从差异>`

## 7. 目录结构（约定）

```
doc-reader-ai/
├── backend/
│   ├── main.py          # FastAPI 入口 + 路由
│   ├── convert.py       # LibreOffice 转换封装
│   ├── extract.py       # PyMuPDF 逐页抽取 + 语言检测
│   ├── llm.py           # chat 模型调用 + 流式封装
│   ├── providers.py     # 多模型路由：chat / vision / image_gen 配置与分发
│   ├── prompts.py       # 提示词模板与拼装
│   └── db.py            # SQLite 建表与访问
├── frontend/                  # React 19 + Vite + TS + Tailwind + shadcn/ui
│   ├── src/components/SubjectSidebar.tsx  # 科目/文件夹/课件树：dnd-kit 拖拽排序+移动+新建
│   ├── src/components/PdfViewer.tsx       # react-pdf 连续滚动 + 按页宽高比渲染 + 页勾选 + 当前页追踪
│   ├── src/components/ChatPanel.tsx       # AI SDK useChat：流式对话 + 保存/回看
│   └── src/lib/api.ts                     # 后端接口封装
├── data/
│   ├── app.db           # SQLite
│   ├── originals/       # 原始上传文件
│   └── pdfs/            # 转换后 PDF
└── docs/                # 本文档、PRD
```

## 8. 开发计划（里程碑，每个都可演示）

| 里程碑 | 内容 | 完成标志（演示脚本） | 预计 |
|--------|------|---------------------|------|
| M1 | PDF 全链路：科目→上传→预览（连续滚动）→勾页→逐页索引→AI 问答（含双语） | ✅ 2026-08-17 | `<1 周>` |
| M2 | PPT/Word 转换接入 + vision 读图兜底 | ⏳ 下一个 | `<3 天>` |
| M3 | 对话存储 + 页码定位回看 + 侧栏整理（拖拽排序/跨科目移动/文件夹）+ 多轮打磨 | ✅ 2026-08-17（M3a 存档回看 + M3b 拖拽整理 + tree 接口扁平化修复 + AI 气泡样式修复） | `<1 周>` |
| P2a | 画图题（程序化绘图 + image_gen 降级，见 1.3） | ✅ 2026-08-23（函数/布尔/电路验证截图已入 docs/screenshots） | 已完成 |
| P2b | 模型与 API 设置：Provider 管理、模型用途绑定、测试连接、默认/备用模型降级 | — | `<2-3 天>` |

## 9. 风险与预案

| 风险 | 概率 | 预案 |
|------|------|------|
| **图片型/扫描课件无文本层**（很多 PPT 整页是图），抽取为空 | 高 | vision 模型读图回填（M2 接入，见 1.3 模型矩阵）；读图失败时标记 `empty_pages` 提示用户该页不可提问 |
| PPT 转 PDF 排版失真（动画/特殊字体） | 高 | 兜底：提示用户手动导出 PDF；只保证常见课件可用 |
| 语言检测误判（中英混排页） | 中 | 词频阈值放宽 + 页面标记允许前端手动改；检测逻辑独立成函数便于调 |
| LibreOffice 在 macOS 上调用踩坑 | 中 | 备选：仅先支持 PDF，M2 顺延 |
| 模型解题/双语服从不达预期 | 中 | 模型可插拔；提示词模板独立文件，实测调优不动业务代码 |
| 用户填错 Base URL / Key / 模型名导致不可用 | 高 | 模型设置页必须提供分能力测试连接；后端返回可读错误类型；保存前可允许未通过但需标红提示 |
| API Key 泄露到前端或 Git | 中 | 接口只返回掩码；`.env`/本地数据库/密钥文件 gitignore；push 前做 secret scan |
| 100MB 大文件转换慢 | 低 | 异步转换 + 前端进度条；超时降级提示 |

## 10. 环境与部署

- 运行环境：macOS / Linux / Windows（本地开发），`python 3.11/3.12 + node ≥ 20`
- 启动方式：macOS/Linux 用 `make dev` / `./dev.sh` 一键起后端；Windows 用 `start.bat`（首次运行自动建 venv 装依赖）；手动方式见 README
- API key 管理：页面写入本地数据库并仅掩码回显；`.env` 仅作为一次性升级迁移输入/回滚材料，不是运行时配置源，二者均不进 git
- 依赖安装：LibreOffice——macOS `brew install --cask libreoffice`、Linux `apt/dnf install libreoffice`、Windows 官网安装包（默认路径自动探测）；Python 包默认走 PyPI，国内网络较慢可换清华镜像（可选项）
