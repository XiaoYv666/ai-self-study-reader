# 需求文档（PRD）

> 项目名：AI 自学阅读器（AI 自学阅读器 + AI 助教）
> 版本：v0.3　更新日期：2026-08-23　状态：已定稿 UI 风格（V2 温暖纸感）；新增侧栏整理、页面尺寸自适应与模型/API 设置需求
> 前置阅读顺序：本文档 →《技术文档-方案.md》→《前端选型调研.md》

---

## 1. 项目概述

**一句话**：面向大学生自学课件（PPT/PDF/Word）的阅读器，左侧预览文档、右侧 AI 对话窗，选中内容即可提问，获得知识点+解题+举一反三的系统性讲解，同时可以通过点击对应ppt页数随时定位到回答内容（回答内容是否需要储存由用户自己决定）

**解决什么问题**：
- 现有产品要么模型不够强（WPS AI 解题一般），要么没有阅读器界面（通用 AI 聊天产品的纯对话模式）
- 用户的prompt编写能力各不相同，不一定能准确依靠AI解决自己的困惑
- 用对话式 AI 自学课件只能反复截图，而且用户不方便翻阅上下文进行回顾复习（上下文加载费时且对话后强制回到最新对话）
- 课件是全英文时，ai对话总是也用全中文/全英文来回答，导致想要标准解答的用户看不懂英文，看全中文能看懂但是又不知道英文答案应该怎么写

**成功标准**（做完后怎么判断"成了"）：
- [ ] 用户新建一个科目菜单
- [ ] 在菜单中上传一份课件
- [ ] 点击那一份 PPT 课件，页面中间能够连续性地看到课件内容（无需翻页）
- [ ] 用户点击课件浏览页面，会自动勾选对应课件页数
- [ ] 用户通过右侧AI对话栏页面与AI对话
- [ ] AI自动根据勾选的课件页数内容，解决用户提问的问题，能给出详细的知识点，进行细致地逐步讲解，同时如果涉及到一些基础知识点，AI会在回答前先为用户回顾基础知识点
- [ ] 用户对话完后，可以选择储存该次对话，将对话结果储存到对应课件页数，用户复习时可以随时点击课件对应的页数来回顾对话
- [ ] 全流程不依赖截图工具，全靠自动勾选并索引识别
- [ ] 检测到对应课件页码的内容为全英文时，AI的回答结果应采用中英文双语，关键知识点必须采用中英文双语，最终的标准解答答案至少要有全英文，解题思路讲解至少要有全中文

## 2. 目标用户

| 维度 | 描述 |
|------|------|
| 是谁 | 大学生（以自己为首个用户） |
| 使用场景 | 考前复习课件、自学新科目课件、日常解题提问 |
| 不服务谁 | 暂不考虑移动端用户、协作编辑场景 |

## 3. 用户故事与功能需求

> 格式：作为【角色】，我想【做什么】，以便【获得什么价值】。
> 优先级：P0 = 核心链路缺一不可；P1 = 核心体验；P2 = 锦上添花。

### P0
- [ ] 作为大学生，我想上传 PDF 课件并预览，以便在浏览器里直接阅读
- [ ] 作为大学生，我想选中 PDF 里某几页，向 AI 提问，以便获得针对性讲解
- [ ] 作为大学生，我想让 AI 为我回顾基础知识点，学习新知识点，同时总结解题思路，按解题步骤讲题并给同类变式（举一反三），以便真正学会而不只是看懂答案

### P1
- [ ] 作为大学生，我想上传 PPT / Word 文件，以便不用手动转 PDF
- [ ] 作为大学生，我想对话保留上下文（多轮追问），以便像和老师对话一样深入
- [ ] 作为大学生，我想让对话有上下文回顾，当前有些问题可能和以前遇到的问题有相似/相反之处，可以教用户如何进行区分/类比
- [ ] 作为大学生，我想让用户能在对话中与AI进行讨论，质疑AI因识别错误/推理错误/生成错误而产生的结果，最终不断改正优化给出正确的结果
- [ ] 作为大学生，我想保存/回看历史会话，并且与课件页码进行定位匹配，以便复习时翻记录

### P2
- [ ] 作为大学生，我想让回答能解决画图类问题，能够依靠推理模型和生图模型给出合适的答案，而不是靠普通的文字描述来指导用户画图
- [ ] 作为高级用户，我想在页面里添加、管理、测试并切换推理/识图/生图模型 API，以便根据成本、稳定性和可用额度自由接入 任意 OpenAI-compatible provider 或本地模型服务

## 4. 功能细节（逐条展开 P0/P1）

### 4.1 文档预览
- 支持格式：PDF（P0）、PPT/PPTX（P1）、DOCX（P1）
- **页面尺寸自适应**（2026-08-17 新增）：课件既有 16:9/4:3 的 PPT 规格，也有 A4 竖版的讲义/试卷。阅读区按每页实际宽高比渲染（上传时取每页 width/height 存库），不做「统一切成 16:9」
- 基本操作：翻页、缩放、连续滚动翻页
- 文件大小上限：100MB
- 不做：批注、编辑、打印

### 4.2 侧栏整理（2026-08-17 新增）
- 科目支持拖拽排序（同级上下移动）；课件支持拖拽排序 + **跨科目移动**（拖进另一科目/文件夹即移动，弹确认防误操作）
- 科目内支持创建**文件夹**（如「第一章」「错题集」），文件夹可嵌套至多 2 层，课件可拖入文件夹
- 移动/重排后保持：勾选状态、页码引用（P12 还是 P12）、已存对话与课件的关联不受影响
- 交叉引用保护：对话回看按 doc_id 关联，与科目/文件夹归属无关，移动课件不丢历史对话

### 4.3 勾选页提问（原 4.2）
- 触发方式：点击课件浏览页面的某一页（可多选） → 右侧ai聊天栏自动定位需识别的页数（即课件被选页） → 选完后用户可以在右侧聊天栏页面与AI进行对话
- 必须带上：课件被选页码（让 AI 知道用户问的是课件的哪个内容）
- 不做：暂不做框选区域截图

### 4.4 AI 讲解（原 4.3）
- 讲解模式：分析知识点，为用户回顾涉及的旧知识点，讲解新知识点 --> 若检测到需要解题，则分析解题思路，做好知识点的引用，给出解题步骤和结论 --> 如果有多种解题方法，则展示多个；如果题目有典型的公式化解题思路，则总结出这类题的公式化解题思路并且告诉用户如何区分出来
- 响应形式：流式输出（打字机效果）
- 模型可切换：AI 对话栏读取页面中已启用且 Provider 已启用的真实 `chat` 模型，显示“模型名 · Provider 名”；默认选 priority 最小、同 priority 时 id 最小的模型。用户切换只影响当前对话的推理，不改动全局优先级，也不影响识图/生图模型
- 提示词要点：
        - 角色设定：一位耐心的大学助教，面向自学课件的学生，默认学生无老师指导                                 
        - 输入约定：系统自动注入被勾选页码的课件内容；AI 只依据勾选页内容+自身知识回答，                       
        引用课件原文时标注页码（如「见 P12」），不臆造课件中没有的内容                                       
        - 问题分流：先判断问题类型（学知识点 / 解题 / 讨论纠错），再选择对应讲解结构                           
        - 教学顺序：涉及基础知识点时，先简要回顾前置知识（标注"前置回顾"），再讲新知识点                       
        - 解题结构：复述题目 → 思路分析（引用用了哪些知识点）→ 分步骤推导（每步编号+依据）→ 结论；             
        有多种解法则全部展示并比较优劣；有公式化套路时总结成模板，并说明如何识别这类题                       
        - 双语规则：检测课件页内容为英文时，关键知识点必须中英对照，标准解答至少给出全英文版本，               
        解题思路讲解至少给出全中文版本；课件为中文时正常用中文                                               
        - 纠错机制：用户质疑回答时，先重新自查推理和识别结果，错了明确承认并给出修正版，                       
        不确定时说明不确定的部分，禁止嘴硬或敷衍                                                             
        - 上下文关联：结合本次会话历史，若当前问题与之前的问题相似/相反，主动指出并讲区分方法                  
        - 输出纪律：步骤编号、公式用 LaTeX、不灌水、答案和讲解分层呈现方便复习时快速定位

### 4.5 模型与 API 设置（P2，高级设置）
- 入口位置：放在侧栏底部或右上角「设置 → 模型与 API」，不作为主学习界面的常驻模块，避免干扰普通学习流程
- 能力分组：用户按用途配置默认模型，而不是只维护一堆模型名
  - 推理模型：用于讲解、解题、总结、纠错对话
  - 识图模型：用于扫描页、整页图片型 PPT、图表/公式截图识别
  - 生图模型：用于画图题、示意图、几何图、复杂非程序化图像生成
- Provider 管理：支持新增/编辑/禁用/删除 Provider；字段包含名称、类型、Base URL、API Key、鉴权方式、额外请求头、备注
- Model 管理：支持在 Provider 下新增模型；字段包含模型名、能力用途（推理/识图/生图）、是否启用、优先级、备注
- 默认与备用：每个能力用途按 priority 从小到大排序，默认使用 priority 最小、同 priority 时 id 最小的启用模型；当前版本单次请求失败会直接返回错误，不自动尝试下一模型
- 当前对话切换：右侧 AI 对话栏只展示启用模型且所属 Provider 启用的 `chat` 配置；切换后请求明确携带 `model_config_id`，无效/禁用/非 chat 配置必须报可操作错误，不得静默回退默认模型
- 测试连接：配置后必须能分别测试推理/识图/生图链路，返回成功/失败、错误原因、延迟和一段简短测试结果
- 密钥安全：前端只展示掩码（如 `sk-****abcd`），接口不返回完整 API Key；导出配置时默认不导出 Key
- 唯一配置源：系统运行时只使用页面保存到本地数据库的模型配置；无已启用模型时明确提示前往「设置 → 模型与 API」配置，不从 `.env` 静默兜底
- 旧配置迁移：升级时仅在模型配置表完全为空的首次启动，将现有 `.env` 中有效的推理/识图/生图配置一次性导入页面数据库；迁移幂等且不得覆盖已有页面配置
- 请求头安全：`Authorization`、Token、Secret、API Key 等敏感额外请求头与 API Key 一样只回显掩码；编辑时留空或原样提交掩码不得覆盖旧值
- 本地优先：配置保存于本地后端，不上传第三方；仍需确保 `.env`、本地数据库和密钥文件不进入 Git

## 5. 非功能需求

| 类别 | 要求 |
|------|------|
| 性能 | 流式首字响应 < 3s；PDF 渲染 100 页内不卡 |
| 隐私 | 文档只存在本地，不上传第三方（模型 API 调用除外） |
| 平台 | 浏览器（Chrome/Edge 优先），本地运行 |
| 成本 | 用户自行配置 API key；成本取决于所选 provider，可优先选择免费额度或本地模型 |

## 6. 竞品参考

| 产品 | 可借鉴 | 不满意的地方（= 我们的机会） |
|------|--------|------------------------------|
| WPS AI | 三格式全覆盖、选中即问 | 模型解题能力一般 |
| NotebookLM | 引用定位、学习指南 | 不支持 PPT、偏总结不偏解题 |
| `<补充>` | | |

## 7. 范围外（明确暂时不做）

- 多人协作
- 移动端
- 知识库/多文档检索（RAG）
- `<补充>`
