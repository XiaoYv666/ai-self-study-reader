# 前端 UI 技术选型调研（2026-08-16）

> 面向「AI 自学阅读器：AI 自学阅读器 + AI 助教」。结果可直接补入《技术文档-方案.md》§1 选型表。
> ✅ **2026-08-16 已联网复核**（npm registry 实查）：react-pdf@10.4.1（MIT，2026-02-25 发布，捆绑 **pdfjs-dist 5.4.296**）、pdfjs-dist 最新 6.2.108、assistant-ui@0.0.110（MIT，2026-08-12 发布）、ai（Vercel AI SDK）@7.0.66（Apache-2.0，2026-08-14 发布）。文中遗留的 `⚠️复核` 标注仅剩 star 数等不影响决策的参考数字。

---

## 一、React vs Vue：最终建议

**推荐：React（Vite + TypeScript + Tailwind v4 + shadcn/ui）+ Vercel AI SDK**

| 维度 | React 路线 | Vue 路线 | 结论 |
|---|---|---|---|
| shadcn/ui 审美生态 | 原生主战场；组件=源码可随意魔改；AI 类产品事实标准 | shadcn-vue 是移植版，更新滞后、教程少 | **React 胜** |
| PDF 渲染组件成熟度 | react-pdf ≈10k★⚠️复核，活跃、由 pdf.js 核心维护者 wojtekmaj 维护 | vue-pdf-embed ≈1.5k★⚠️复核，单人维护、pdf.js 版本长期滞后 | **React 胜** |
| AI HTML 原型迁移成本 | Tailwind class 1:1 保留，div→JSX 机械转换；shadcn 组件与原型零件一一对应 | 同样可行，但 v-for/:bind 多一层语法转换 | React 略胜 |
| AI 聊天参考项目可抄性 | lobe-chat、ai-chatbot、assistant-ui 全是 React；assistant-ui 直接是可安装的组件库 | Open WebUI 是 Svelte（另起炉灶）；Vue 系没有像样的 AI chat 组件库 | **React 大胜** |
| 流式对话（SSE） | AI SDK `useChat` 一行拿打字机效果+中断+多轮 | AI SDK 也有 `@ai-sdk/vue`，但文档/示例少一个数量级 | **React 胜** |

补充决策点：
- 技术文档里「Vue3 暂定」的潜在理由是 `@vue-office/pdf` 等 Vue 文档组件，但本项目 PPT/Word 已定走 LibreOffice→PDF 转换，**前端只需渲染 PDF 一种格式**，Vue 该优势不成立。
- 「Claude 出 HTML 原型」：AI 设计工具常能输出 Tailwind HTML；React+Tailwind 迁移损耗最小；shadcn 的 button/card/sidebar/dialog 直接对应原型每个零件。
- 深色模式：shadcn 用 CSS 变量 + next-themes 思路，两行配置，React 生态最成熟。

## 二、PDF 渲染组件对比（重点验证部分）

统一前提：PPT/Word 已转 PDF，前端只渲染 PDF；核心交互 = 连续滚动 + 点击页面勾选 + 拿页码 + 按页渲染回调。

**结论：React 线用 react-pdf（堆叠渲染实现连续滚动）；可选 react-pdf-viewer 试水（内置连续滚动，但注意 license 水印条款）。**

| 需求点 | react-pdf | vue-pdf-embed | pdfjs-dist 直接用 | pdf.js 官方 PDFViewer |
|---|---|---|---|---|
| 连续滚动 | ❌ 无内置；标准做法=滚动容器堆叠 N 个 `<Page>`（社区标配） | ❌ 无内置，同上堆叠（README 有示例） | ❌ 自己写 | ✅ 内置 continuous scroll |
| 拿页码 | ✅ `<Document onLoadSuccess>` 给 numPages；`<Page pageNumber>` | ✅ `@document-loaded`(numPages) / `@page-loaded`(页码) | ✅ 自己拿 | ✅ `currentPageNumber` API |
| 滚动位置→当前页 | ⚠️ 自写 IntersectionObserver 标定（约 20 行） | ⚠️ 同上 | ⚠️ 同上 | ✅ 内置 page change 事件 |
| 按页渲染回调（勾选页交互地基） | ✅ `<Page onRenderSuccess>`（v7+）、`onLoadError` 逐页容错 | ✅ `@page-rendered` 事件 | ✅ render 回调自传 | ✅ 有 page 事件 |
| 点击页=勾选（可多选） | ✅ 每页外包 div 绑 onClick，页码即 index | ✅ 同上 | ✅ 同上 | ⚠️ 在 viewer 上包点击层略绕 |
| pdf.js 版本兼容 | **已证实：v10.4.1 捆绑 pdfjs-dist 5.4.296**（pdf.js 5.x 线，worker 用 ESM 方式加载；写法注意 `pdfjs.GlobalWorkerOptions.workerSrc` 指向 5.x 的 worker 文件） | ⚠️ **最大短板**：长期落后最新版（v1.x 停 3.x，v2.x 才跟进 4.x），与 5.x 兼容未知 | ✅ 自己控版本，最干净 | ✅ 官方同步 |
| 维护活跃度 | ✅ 2025 持续发版 | ⚠️ 单人维护，以季度计 | ✅ Mozilla 官方 | ✅ Mozilla 官方 |
| Star / License | ≈10k★ / MIT | ≈1.5k★ / MIT | ≈50k★ / Apache-2.0 | 同 pdf.js |
| 100 页性能 | ⚠️ 建议 IntersectionObserver 懒挂载或 react-window 虚拟滚动（社区有现成例子） | ⚠️ 同上 | ⚠️ 全自己控 | ✅ 自带虚拟化（只渲染视口附近页） |

要点说明：
1. **勾选页交互无黑魔法**：每页渲染成一个可点击 div（页码=index）+ IntersectionObserver 算当前页，react-pdf/vue-pdf-embed 都能做。
2. **vue-pdf-embed 的 pdf.js 版本滞后 + 单人维护**是长期风险——这本身就是选 React 的加分项。
3. **pdfjs-dist 直接用**适合极致控场派，本项目不需要这个自由度，react-pdf 省 200 行样板。
4. **pdf.js 官方 PDFViewer**（`pdfjs-dist/web/pdf_viewer`）是唯一开箱即「连续滚动+页码+虚拟化」的裸方案，但样式是 Firefox 阅读器风，改造成项目审美工作量大。
5. **react-pdf-viewer**（react-pdf-viewer.dev，≈13k★⚠️复核）：内置连续滚动/页码/缩略图，最接近成品阅读器体验；但 **license 非 MIT**：免费版需保留「Powered by react-pdf-viewer」水印（商用付费）⚠️复核条款——个人本地项目可用，介意水印则弃。
6. 逐页文本（发 AI 用）走后端 PyMuPDF 抽取，前端无需文本层，上述选择不受 text-layer 差异影响。

## 三、AI 聊天界面开源参考（看设计不 fork）

### lobe-chat（lobehub/lobe-chat）
- ≈55k★⚠️复核 / **MIT** / Next.js + React + Ant Design v5 + Zustand + Tauri（桌面）
- 交互亮点：会话侧栏 + 消息操作（复制/重新生成/编辑/点赞）；消息气泡的圆角与间距细节；代码块高亮+一键复制；流式光标动画；渐变主题 + 暗色模式；角色头像体系。
- **注意**：它的精致来自深度定制 antd（自研 LobeUI token），**不是 shadcn**——抄「设计语言」，别抄技术栈。
- 可抄：消息区布局、打字机光标、消息操作条、主题 token 化。

### Open WebUI（open-webui/open-webui）
- ≈100k★⚠️复核 / **BSD-3-Clause 含品牌条款**（2025 起要求保留 Open WebUI 品牌标识）⚠️复核 / Svelte 5 + SvelteKit + Tailwind（shadcn-svelte）+ FastAPI 后端
- 交互亮点：极致简洁的消息流；Markdown/LaTeX/代码高亮渲染；模型切换器；左栏文档管理；暗色模式对比度处理；流式走 WebSocket（本项目用 SSE，更简单）。
- 可抄：消息渲染（markdown+公式）、输入区设计、暗色模式配色。

### 其他高星项目（2-3 个以上）

| 项目 | Star⚠️复核 | License | 技术栈 | 参考价值 |
|---|---|---|---|---|
| **Vercel ai-chatbot**（vercel/ai-chatbot） | ≈15k★ | Apache-2.0 | Next.js + AI SDK `useChat` + Tailwind + **shadcn/ui** | **最该看**：SSE 流式标准写法、消息气泡、输入区、会话列表，结构教科书级、代码量小 |
| **assistant-ui**（assistant-ui/assistant-ui） | ≈5k★ | MIT | React + Tailwind + shadcn/ui + AI SDK | 不是项目是**组件库**：Thread/Message/Streaming 组件开箱即用，可直接 `npm i` 省 80% 聊天 UI 工作量 |
| LibreChat（danny-avila/LibreChat） | ≈35k★ | MIT | React + MUI + Vite + Express + MongoDB | 功能最全的 ChatGPT 克隆（多模型/文件/插件）；MUI 审美偏重，参考功能组织 |
| （备选）Hugging Face chat-ui | ≈10k★ | Apache-2.0 | SvelteKit | 极简聊天参考，取舍即可 |

结论：**设计细节抄 lobe-chat，简洁性抄 Open WebUI；流式工程实现抄 ai-chatbot；能直接引用的用 assistant-ui**。SSE 打字机 = Vercel AI SDK `useChat`（OpenAI-compatible base_url 指向用户自己的 provider 即可），2025 年事实标准。

## 四、组件库推荐

**React 线：shadcn/ui + Tailwind v4 —— 2025-2026 仍是审美标杆，无悬念推荐。**
- Tailwind v4（2025-01 发布）：CSS-first 配置（`@theme`）、零 config 文件、Vite 官方插件。
- shadcn/ui 2025 全面适配 Tailwind v4 + React 19，registry 组件 `npx shadcn add` 直接装。
- 「组件=你的源码」= 全量自定义权，正好匹配「配色/间距/交互细节亲手调」的审美诉求；深色模式 = CSS 变量两行切换。
- 备选无需深究：MUI / antd（后台感）、Chakra（生态收缩中）。

**Vue 线现状（供对比，不推荐）**：
- **Naive UI**（≈18k★⚠️复核，MIT）：Vue3 审美第一梯队，TS、主题 token 化、暗色模式好；但不用 Tailwind、是完整设计系统，深度魔改不如 shadcn 自由，且无 AI 聊天组件生态。
- **shadcn-vue**（unovue/shadcn-vue，≈5k★⚠️复核，MIT）：React 版移植，底层 reka-ui（原 radix-vue）；能用，但更新滞后于 React 版，踩坑时社区答案少。
- 结论：「Vue 线能做但天花板低一档」的判断在 2025-2026 依然成立。

## 五、落地：补进技术文档选型表 + 开工复核清单

技术文档《技术文档-方案.md》§1 更新建议：

| 层 | 选型 | 理由 |
|---|---|---|
| 前端框架 | **React 19 + Vite + TypeScript**（替代「Vue3 暂定」） | shadcn 生态 + AI chat 参考全在 React；HTML 原型迁移损耗最小 |
| UI | **Tailwind v4 + shadcn/ui**（CSS 变量深色模式） | 审美标杆、源码级自定义 |
| 流式对话 | **Vercel AI SDK `useChat`**（OpenAI-compatible provider） | 打字机/中断/多轮开箱即用 |
| PDF 渲染 | **react-pdf**：堆叠 `<Page>` 实现连续滚动；IntersectionObserver 算当前页；每页 div 绑 onClick 做勾选；100 页以上加懒挂载/虚拟滚动 | MIT、活跃、能力全覆盖 |
| 可选试水 | react-pdf-viewer | 内置连续滚动+虚拟化，但 license 有水印条款 ⚠️复核 |

**复核清单执行情况（2026-08-16）：**
1. ✅ npm registry 实查：react-pdf 10.4.1 / pdfjs-dist 6.2.108 / assistant-ui 0.0.110 / ai 7.0.66（见顶部）
2. ⏭️ star 数未查——不影响任何已定决策，跳过
3. ⏭️ react-pdf-viewer license 未查——已决定不用它（水印条款），无需复核
4. ✅ shadcn CLI 4.18.0（2026-08-13）、tailwindcss 4.3.3（2026-07-16）——均为活跃最新线，直接装
